# ============================================================
# 运行期依赖（跑 剪映伴侣.py 就需要）
#   版本号是本项目实测通过的组合；上游大版本变动不保证兼容。
# ============================================================
pillow==12.3.0          # 【必需】ImageGrab 截屏，用于「时间轴是否真的清空了」的像素自检

# 【可选但强烈建议】被其他窗口（微信/浏览器）全遮住时点任务栏按钮的唯一可靠通道。
#   源码里的 import 包在 try 里，装不上也能跑，只是少了这条兜底。
pywinauto==0.6.9        # 会自动带上 comtypes、pywin32

# tkinter 是 Python 自带的（Windows 官方安装包默认勾选 tcl/tk），不在这里列。

# ============================================================
# 开发/构建依赖（只在你要自己打包或跑测试时才装）
# ============================================================
pyinstaller==6.22.2     # 打 onefile exe
pyflakes==3.4.0         # 静态检查（CI 级：源码里只允许留一个已知的未使用变量告警）

# ------------------------------------------------------------
# 安装：
#   pip install -r requirements.txt
# 构建顺序（务必照做，否则安装包里会嵌到旧 exe）：
#   四套测试 → pyflakes → PyInstaller → _verify_exe.py → 重启伴侣.py → ISCC
# 详见 docs/开发与构建.md
# ------------------------------------------------------------
