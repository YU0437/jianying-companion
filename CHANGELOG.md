# 更新日志 · Changelog

本项目的版本号跟着 `version_info.txt` 走。日期为本地时间。

---

## [1.6.0] — 2026-09-20 「球心换成会动的小人」+ 一个隐形渲染 bug

用户原话：「把剪的图片换成这个，没运行时就不动，正在运行时就动」（附 23 帧动图）。

球心那个"剪"字换成用户给的动图：**待机时静止在第一帧，干活时才跳**。

### 新增

- ★ **球心动图**（`ball_avatar.gif`，23 帧 / 每帧 100ms / 250x291）。同一张脸同时
  出现在**球心**和**展开后胶囊左侧的图标位**——不能让"一悬停展开，球上的小人变回剪字"。
  取图三道工序：① 每帧**等比装进正方形**（**一个像素都不裁**：主体是全身，裁满必然
  砍掉头或脚，而 40px 的球心里少一截就认不出是个人），两侧用**画面自己的边缘列**
  拉伸补满（补的是边缘色，裁圆后看不出接缝）；② **先 resize 再裁圆**（反过来的话
  RGBA 缩放假发边，圆周上会带一圈背景色晕）；③ 按 (帧号, 直径) 记忆化，
  稳态下每帧只是一次 `paste`。
- ★ **判据 `is_working(kind, hold)`**：`busy + hold=0` 才算"正在运行"。
  `kind="busy"` 有 25 个调用点，其中一半是"导出守望已开 / 剪映路径已设置 /
  已将嵌入剪映"这类**几秒后自己收回的通知** —— 那些不该跳舞。
  两种情况的 kind 和配色完全一样，**只有 `hold` 分得开**。
  （试过并否掉的判据：`_cancel is not None` 会漏掉"取文件中 / 体检 / 导出守望"
  三条不建令牌的路，还会把"正在启动剪映…"这种**等用户**的状态算成在跑；
  `pct is not None` 会漏掉"正在中止…"这种没有百分比的。）
- `ok` / `err` / `ask` **保留原字形**（✓ × !）：那三个字是一眼可读的信号，
  不能被小人顶掉。干的活结束回到 ok → 小人自动站回第一帧。
- 帧号只走**主循环那次判定**：图是 10fps、33ms 的跟随循环是 30fps，
  帧号没变就不重画（不然 3 倍白干）。

### 界面

- 跑起来时头像**缩到进度环内沿以内**（48px 球：待机 40px → 干活 25px）。
  这不是审美微调：环画在半径 17px 上，而 40px 的头像半径 20px，
  不缩的话进度环会被**整个盖住**——屏幕上就是"正在跑但看不到进度"。
  缩完之后环正好成了头像的外圈，读起来像"带进度环的头像"。
  ★ 代价：`busy` 的**百分比数字不再画在球心**（进度改由环 / 底部进度条承担，
  数字仍由 `ball_label` / `pill_glyph` 给出，悬停展开时在图标位看得到）。

### 修复（★★ 顺带挖出来的隐形 bug，先于本批就存在）

- **球顶那圈亮线会"越描越白"，最后烧成纯白**。`_vgrad()` 把**缓存对象本身**
  交出去，而 `_body()` / `_do_card()` 直接在它上面 `paste` 白线 ——
  同一份渐变被复用几次，那圈白就叠几次（`alpha=74` 贴两遍 = 50%、五遍 = 82%、
  四十遍 = **255**）。实测：同一个球连渲 40 次，球顶从 `(121,121,124)`
  一路烧到 `(254,254,254)` —— 屏幕上就是"球顶上箍了一圈白塑料"。
  早先没暴露，是因为渲染结果自带缓存挡着"完全相同的参数"；
  而**任何参数一变就会重建**（跑起来时 `pct` 每 1% 一变、悬停时 hover 连续变、
  这一批之后动图每 100ms 变一帧）——它专挑**最该好看的动态过程**发作，
  静态截图反而看不出来。修法：`_vgrad()` 交出**副本**（缓存保留，只是不再把
  可变状态漏出去）。实测修完连压 40 帧球顶**一个像素都不漂**。

### 修复（回归守卫）

- `test_guard.py` 新增 ㉴ 组 15 条：判据取值表、判据不许回退成 `pct`/`_cancel`、
  素材在仓库里、**spec 的 `datas` 带上了它**、时间轴按 GIF 逐帧时长、
  帧号进缓存键、头像直径、**跑起来时进度环一个像素都没被挡**（逐像素断言）、
  待机头像正好 40px 且不越出球、素材缺失时退回画字、语义层一个字没动、
  球心与图标位同脸、`_vgrad` 必须交副本、`_body` 两次必须逐字节相同、
  连压 30 帧不许漂。
- `test_gui_smoke.py` 新增 11 条：状态 → 帧号这条链（待机 0 / 开跑从 0 起 /
  250ms 后第 2 帧 / 收工归零 / 三种"提示"不算在跑 / ok·err·ask 不画），
  以及**帧号真的交给了渲染层**（把 `_ur.render` 换成探针，抓 kwargs ——
  "算了但没传下去"是这一批最容易犯的错，屏幕上只表现为"一直定格"）。
- `_verify_exe.py` 补：新名字核验 + **动图资源在包里**（这条只能查归档 toc ——
  二进制资源不在 code object 里，名字核验永远看不到它；`datas` 漏带的话
  全部名字都在、全部取值都对，只有球心永远是个剪字）。

---

## [1.5.0] — 2026-09-20 「质感返工」：把"看着廉价"拆成五个可测的像素问题

用户原话：「ui 还是丑，看着廉价」。

这句话不像需求，但它完全可以被拆成**具体的像素事实**。这一轮没有凭感觉调色，
而是先找出"到底哪几个像素在让人不舒服"，每一处都用实测数据定点，再逐条修掉、
并把修好的性质**钉成回归断言**（`test_guard` 新的一组 ㉳，15 条）。
上一轮的问题是"配色不够好看"——这一轮发现根本不是配色问题。

### 修复（按"有多刺眼"排序）

- ★★★ **卡片外面的那一圈"方框"**（最刺眼的一处，也是真 bug）。
  卡片底图里的投影遮罩取的是**整张画布**尺寸，而不是卡片本体尺寸 ——
  模糊之后的尾巴直接顶到窗口四边，被矩形窗口**硬切一刀**：
  卡片周围挂着一圈**硬边深灰"板子"**（实测：窗口最外 10px 的 alpha 恒为 **118**，
  也就是一整圈 46% 的黑）。在浅底上看得一清二楚（`shots/v_card_cheap_vs_fixed.png`），
  在剪映暗底上则是"卡片像贴在一张灰卡纸上"。
  同时那圈 alpha>0 会把窗口外圈全变成命中区 —— **白吃一圈鼠标**。
  修法：遮罩只取本体（`_mask(w*SS, h*SS, rad)`）、缩回本体尺寸再贴到画布上
  （球那条路径本来是对的，卡片漏了这层缩放；现在两边共用
  `SHADOW_BLUR/SHADOW_ALPHA/SHADOW_DY` 同一组常量）。修完最外一圈 alpha **= 0**。
- ★★ **125% 屏上被系统整体拉伸 1.25 倍**（DPI 虚拟化）。
  进程没声明 DPI 感知 → 我们精心 4× 超采样出来的位图会被 Windows
  **双线性拉伸**一遍，中文笔画直接糊成灰边。修法：模块级、**任何 Tk 窗口之前**
  声明 `PER_MONITOR_AWARE_V2`（三级回退到 shcore v1 / `SetProcessDPIAware`）。
  ★ 顺带必须补的一件事：声明之后 Windows 不再替我们拉伸，**换显示器就得自己
  重新取 dpi**（本机主屏 125% / 副屏 100%），否则球拖到副屏会大 25%。
- ★★ **文字跟着形状一起 4× 超采样 → 缩回后发虚**。
  中文字形在 4 倍画布上排版，落点乘 4 除不尽 → 降采样成灰边。
  修法：**文字在 1× 上排版**（`draw_text_1x`），按掩码 NEAREST 放大到 SS 再贴 ——
  清晰度的本源是"落在 1× 整像素上"（`int(round(x1)) * SS`）。
- ★ **球在剪映暗底上"消失"**：旧球面渐变 `(46,46,54)→(30,30,35)` 和剪映暗底
  `(26,26,28)` 几乎同色，球下半圈直接没了。修法：抬亮到 `(68,68,79)→(36,36,43)`
  （比剪映底高 20 / 4 级以上），并给卡片再亮一档 `(76,76,86)→(44,44,52)` ——
  **靠明度分层，不靠描边**。
- ★ **进度环变成"双层描边"**：明亮轨道扣在已经抬亮的球面上 = 两道同心亮边，
  读起来很廉价。修法：凹槽改用**暗色** `(0,0,0,88)`，让它像"陷进去"。

### 界面（其他）

- 投影统一规矩：**α<2 硬截断到 0**。人眼看不出 2/255，但 Windows 的分层窗口
  命中测试照样把它算成"不透明"→ 白吃鼠标；归零后边界不再硬切投影。
- README 预览图 `docs/ui-preview.png` **重出**（`_probe_readme2.py`）。
  旧图上的**紫边**是上一版"色键抓屏"路径的产物 —— 那条路已经随
  `UpdateLayeredWindow` 上屏一起退役，现在直接取 `_blit()` 收到的那张位图，
  一个像素都不用猜。

### 修复（回归守卫）

- `test_guard.py` 新增 ㉳ 组 15 条：DPI 声明位置、缩放合成唯一出口、
  换屏重取 dpi、`_scale_follow` 命名、1× 排版、投影外圈 alpha 全 0、
  形状对剪映暗底的亮度差、卡片比球亮一档、进度环凹槽是暗色、
  `ring_inset` 单一来源。**每一条回退都会让画面重新"发虚/发脏"。**
- 断言写法的两处坑（已记进 `docs/开发与构建.md`）：
  ① 查源码找 `.Tk()` 会命中说明注释 → 断言假红，要 `find("self.root = tk.Tk()")`；
  ② 把实测值塞进 `check()` 的 `want` 参数 → 报错信息变成"期望=(68,68,79) 实际=True"
  这种读不懂的对照，正确做法是 `got` 给 bool、实测值写进**名字**里。

---

## [1.4.0] — 2026-09-20 「界面重做」（iOS 风）+ 一轮界面回归修复

用户原话：「悬浮窗 ui 设计感差，非常僵硬（而且悬浮球旁边还有丑陋的边框，不好看）…
ui 设计可以抄一下 iPhone」「点击后要先问一下用户是否准备开始导出」
「悬浮球的通知应该是以小对话框形式在头顶浮现」。

### 界面（重做）

- **球/胶囊外面不再有黑方块**：窗口底色改成色键再让 Windows 抠掉 ——
  圆形之外**真的透明**（顺带鼠标穿透：以前球四周那四个角会吃掉点击）。
- 整套配色换成 iOS 深色系统色（系统蓝/绿/红/橙 + 分层底色 + 发丝描边）；
  圆角改成胶囊尺度；进度弧内嵌一圈（以前一半弧线跑到球外面，像糊了一块橙色）。
- 胶囊左侧改成 **28px 圆形图标位**，文字从图标右侧起；干活时图标位显示**进度数字**。
- 底部进度条从方头矩形改成**胶囊**。
- **通知/确认改成"球头顶的小卡片"**：iOS 风格、圆角、无边框、淡入滑入；
  确认卡带分隔线和两个按钮（悬停有底色反馈）。**不再弹系统 MessageBox。**
- **球可以拖着走**，松手记住位置（右键「重置位置」回角落）。
- **点「一键导出」先弹确认卡**（会清空原时间线，值得先看一眼再动手）。
- 悬停判定加了光标轮询兜底（窗口频繁移位时 tk 的 Enter/Leave 会丢）。
- **README 的界面预览图改成脚本重出**（`docs/ui-preview.png`，五个状态真机渲染）：
  旧图是手工拼的，界面改完它还挂着旧配色 —— 文档一旦会"说谎"就不如没有。
  重出脚本踩了两个坑（都在 `docs/开发与构建.md` 记了）：① 按多边形"挖图"会因为
  `GetWindowRect` 的外框和画布内容差几像素而在角落留下背景；②
  `attributes("-transparentcolor", "")` **不抛异常但不生效** ——
  正解是把它换成一个界面绝不会用到的颜色，品红窗口底就露出来了。

### 修复（这一轮真正的重点：界面"说不清话"）

- ★★★ **胶囊宽度算错 13px**：`_fit_width()` 还在按旧布局（文字从 36px 起、右边留 10）
  预留宽度，而新布局是 47 / 12 —— 于是文案永远放不下，被省略号吃掉**关键词**：
  「草稿备份没做成，导出后无法自动**还原**」曾经只剩「…导出后无法自动…」。
  修法：抽出 `ui_metrics()` 当**唯一**的度量出口，两处共用。
- ★★ **不再出现「… …」**：源码里大量文案自带省略号（「批量处理中…」），
  `ellipsize()` 又补一个 → 屏幕上像一串乱码方块。改成幂等（先剥尾部省略号再决定）。
- ★★ **中文折行禁则**：卡片正文里标点会被挤到行首**单独成行**，
  居中排版时屏幕正中间就浮着一个孤零零的「。」（放大看像个小圆环，
  我们一度误判成"屏幕残影"）。现在标点悬挂在上一行、左括号不落行尾。
- ★ **确认卡按钮的悬停把文字洗成默认色**：矩形与文字共用一个 tag，
  移开时 `fill=""` 把「开始」变成黑字/看不见，光标在两者间移动还会反复闪。
  现在底色高亮与文字颜色分开、按坐标判定热区（整块按钮都可点）。
- **通知卡正文折行**（最多 3 行）：以前固定 64px 单行，"要用户做的事"会被砍掉半句。
- 卡片收窗改成先 `withdraw()` 再 `destroy()`（这个窗口组合有残影风险，防御性写法）。

### 测试基线

`test_guard` **637**（+13）· `test_gui_smoke` **246**（+22，第二十一批漏改 `dot_item`
导致 smoke 曾整体崩掉，这一轮补齐）· `test_settle` 7 · `_test_backup_restore` 39，全绿。

---

## [1.3.0] — 2026-09-20 「全自动导出」（实验）

到此为止，流程里唯一剩下的手工活是：拖入、点导出、等导完、点还原。
这一版（**实验性、默认关**）把这段也接上了 —— 开了它，从「一键导出」到
"mp4 落盘 + 草稿还原"就是真正的**一次点击**。

### 新增

- **全自动导出（实验）**（右键菜单开关，默认关）：流程走到「等你导出」后，
  伴侣自动：拖入时间线 → 发导出快捷键（读你剪映的实时绑法，默认 Ctrl+E）→
  回车确认 → 守望导出完成 → **自动还原草稿**。
- 与 1.1.0 的守望、1.2.0 的批量队列完全兼容：批量模式跑完每份后同样可以全自动。

### 安全设计（为什么它敢叫"实验"还默认关）

- 导出对话框**不是我们画的**，每版都可能变 —— 所以每一步都有"交回手动"的退路：
  拖入不成 → 交回手动拖；导出窗口没出现 → 交回手动导；回车两次窗口还不关 →
  提示你点一下「导出」，伴侣继续守望（你什么时候导完它都接得住）。
- 全程可中止：中止 = 立即退回手动模式，**不做任何多余动作**。
- 走通后自动还原走的是和手动「还原草稿」**同一条代码路**（写前备份、写后快照）。

---

## [1.2.0] — 2026-09-20 「批量队列」

草稿不止一份时，以前每份都要把整条流程来一遍。这一版起，**一次排队 N 份**，
重活全部自动做，导出仍由你掌握。

### 新增

- **批量处理草稿…**（右键菜单）：点开时现扫草稿目录（有界扫描，3 秒封顶），
  列出最多 8 份「带预合成素材」的草稿（按最近使用排序），可以单选也可以「全部处理」。
- **逐份自动**：自动打开草稿 → 备份 → 预合成 → 等落盘 → 写登记 → 重启进入 →
  清空原时间线 → 落盘。跑完一份自动接下一份，**每份跑完都处于"打开即可导出"的就绪态**。
- **汇总报告**：结束后弹窗列出每份草稿 ✅/⚠️ 的结果，球上只放一句结论。
- **随时中止**：任何时刻中止 = **还原当前这一份**并停下，后面的草稿一律不再碰
  （与单份流程"停 = 还原"同一个语义）。

### 安全设计

- 清空时间线前的草稿名核对与单份流程同样铁面：打开的不是目标草稿就**绝不碰时间线**。
- 每份草稿的备份相互独立；备份没做成的草稿**直接跳过**（不动没备份的草稿）。
- 预合成登记没写进去的草稿**跳过清空**（清了也导不出来，不如保持原样）。
- 清空后发「返回草稿页」持久化（快捷键表里没有"保存"，这是全流程依赖的唯一落盘通道）。

---

## [1.1.0] — 2026-09-20 「导出守望」

流程的最后一段人工是：去剪映里导出，导完**还得记得回来点还原**。
这一版把"记得回来"也消掉了 —— 导出窗口一关，伴侣主动提醒你还原。

### 新增

- **导出守望**（默认开，菜单可关）：流程进入「等你导出」后，伴侣在后台**只观察**
  剪映的导出对话框；对话框"出现 → 消失"（= 导出流程走完了，或被取消）就主动
  把悬浮球弹出来：「检测到导出完成 · 左键一键还原」。
- 菜单新增「导出守望（导完提醒还原）」开关，关闭即回到老行为。

### 安全设计（和"自动拖拽"同一个哲学）

- **只观察、不动手**：对话框消失也可能是用户取消了导出 —— 分不清就不猜，
  还原必须用户点头（左键 / 右键），绝不自动杀剪映写草稿。
- **失败开放**：窗口枚举出任何异常一律当"没看见"。漏提醒一次的代价 ≈ 0，
  误报一次（没导完就喊还原）会打乱用户节奏 —— 白名单尺寸带宁可漏检。
- 守望线程随时可停：开始还原 / 起新一轮 / 退出伴侣都会立刻收摊；
  不在「等你导出」状态时，迟到的提醒会被直接丢掉。

---

## [1.0.0] — 2026-09-19

首个公开版本。核心是「把预合成前后那十几步手工操作变成一次点击」，
并且在每一步都**用像素/指纹/窗口状态自检**，做不到就如实报失败。

### 功能

- **一键导出**：全选 → 建复合片段 → 预合成 → 等产物落盘 → 等剪映保存草稿
  → 杀进程重开 → 选中文档 → 交给用户拖入导出
- **草稿备份与还原**：预合成**之前**把草稿元数据（`.json`）备份到
  `桌面\剪映预合成导出\_备份\`；导出完成后一键**还原到预合成之前**
- **暂终止**：流程运行中左键点球即可中止，**中止后自动还原草稿**；
  「等你导出」阶段也能"放弃本轮"。连预合成都还没发出去时只停下、不碰草稿
- **悬浮球界面**：46px 小圆球，悬停平滑展开；需要用户动手时自动展开，两行文案
  （第一行 = 你要做什么，第二行 = 为什么）；干活时球外圈有进度弧
- **安装时零配置**：安装收尾自动认环境（剪映路径 + 草稿目录）并写进配置；
  草稿位置**直接读剪映自己的设置**，不靠猜常见路径
- **环境体检**：一键检查缺什么（剪映没装 / 没运行 / 草稿目录找不到 / 桌面写不进去）
- **求助回路闭环**：菜单里能直接打开运行日志、使用说明、草稿备份文件夹；
  路径认不出来时能手动指（剪映路径 / 草稿目录），不用手改 JSON

### 安全 / 红线

- **不碰任何视频文件**：预合成产物 `Resources\combination\<GUID>_video.mp4`
  全程字节不变；不解码、不转码、不解密、不改写、不删除
- **不加解密依赖**：早期"缓存解密/还原明文"方向的代码已**物理删除**
- **不联网**：程序内没有任何网络代码
- **只写草稿目录里的 `.json`**：还原功能唯一会写文件的地方，且写前备份、写后再存一份快照
- 改用户剪映配置（预合成快捷键）之前**先备份**，并提供一键还原入口
- 全部行为有测试守着：`test_guard.py` 会直接扫源码断言"这些函数不存在"

### 兼容与实测

- Windows 10 / 11 x64
- 剪映专业版 **11.4.1.14443** 实测通过
- 关键已知问题与规避：
  - 「媒体格式不支持」= 剪映没保存草稿导致预合成登记没落盘 → 伴侣会强制让剪映保存一次
  - 「打开草稿」**只能双击首页卡片**（剪映没有对应的快捷键命令）
  - 自动拖入时间线**默认关闭**（手动拖更稳），可在菜单里打开

---

## 版本约定

- `version_info.txt` 里的 `FileVersion` / `ProductVersion` 必须和安装包文件名一致
- 每次发版：四套测试全绿 → `py_verify`（`_verify_exe.py`）→ 打 exe → 出安装包 →
  推代码 → 在 [Releases](../../releases) 里附上 `剪映伴侣-Setup-x.y.z.exe`
- 推代码本机要 `git -c http.sslVerify=false push origin main`（本机查不了
  GitHub 证书链的吊销状态，不关必失败）；发 Release 用 `python _release.py vX.Y.Z <exe>`
  （本机没有 `gh`，脚本自己走 REST，正文直接取本文件里 `## [X.Y.Z]` 那一段）
